name: CI

on:
  push:
    branches: [testing]
  pull_request:
    branches: [testing]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Vitest unit tests
        run: npx vitest run --reporter=dot

      # Optional: Run Cypress end-to-end tests (requires dev server)
      - name: Start dev server
        run: npm run dev &
        env:
          CI: true
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          start: npm run dev
          wait-on: http://localhost:5173
          wait-on-timeout: 60
